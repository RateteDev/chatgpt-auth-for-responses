# エラー対応

401が返ったら `auth.forceRefresh()` でトークンを更新し、`auth.clientOptions()` を取得し直します。OpenAI SDKのクライアントは古い `apiKey` を使い続けるため、新しいトークンで作り直してから再試行してください。画像クライアントは呼び出すたびに認証を確認します。

400が返ったら [Responses APIの入力条件](responses.md) を確認します。画像APIで断続的に404が返る場合は、時間をおいて再試行してください。

エラー本文を共有する前に、認証情報が含まれていないか確認してください。
