# 障害を調べる

401なら、`auth.forceRefresh()` で更新してから `auth.clientOptions()` を取得し直し、新しいアクセストークンでクライアントを作り直して再試行します。画像クライアントは認証を呼び出し時に解決しますが、OpenAI SDKのクライアントは生成時の `apiKey` を使うため、作り直しが必要です。

400なら [responses.md](responses.md) の入力制約を確認します。画像APIの断続的な404は時間をおいて再試行します。エラーメッセージには認証情報が含まれ得るため、共有前に本文を確認してください。
